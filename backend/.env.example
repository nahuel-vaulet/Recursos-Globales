# ─── Database ──────────────────────────────────
# PRODUCTION (Render → Supabase PostgreSQL):
# Set DATABASE_URL in Render environment variables.
# Format: postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
DATABASE_URL=

# LOCAL (XAMPP → MySQL — used only when DATABASE_URL is empty):
DB_HOST=127.0.0.1
DB_NAME=erp_global
DB_USER=root
DB_PASS=

# ─── JWT Auth ──────────────────────────────────
JWT_SECRET=change-me-to-a-random-64-char-string
JWT_EXPIRATION=3600

# ─── CORS ──────────────────────────────────────
# Not needed when frontend/backend are on same domain (Render Docker)
# Set this only if frontend is deployed separately
CORS_ORIGINS=*

# ─── App ───────────────────────────────────────
APP_ENV=production
APP_DEBUG=false
